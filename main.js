const EDITOR = document.getElementById('editor'),
      CODE = document.getElementById('code');
/*
var pos = 0;

class thum {
    constructor(before) {
        this.before = before;
        this.el = document.createElement('div');
        this.el.classList.add('thum');
        CODE.insertBefore(this.el, this.before);
    }
    delete() {
        this.el.remove();
    }
}
var ci = CODE.innerHTML;

document.addEventListener('keydown',(e) => {
    if (e.keyCode == 8) {
        if (ci.substring(ci.length-2,ci.length) == '\n') {
            ci = ci.substring(0,ci.length-2);
        } else {
            ci = ci.substring(0, ci.length-1);
        }
        CODE.innerHTML = ci.replace(/\n/g,'<br />');
    } else if (e.keyCode == 13) {
        ci += '\n';
        CODE.innerHTML = ci.replace(/\n/g,'<br />');
    } else {
        ci += String.fromCharCode(e.keyCode);
        CODE.innerHTML = ci.replace(/\n/g,'<br />');
    }
});*/

